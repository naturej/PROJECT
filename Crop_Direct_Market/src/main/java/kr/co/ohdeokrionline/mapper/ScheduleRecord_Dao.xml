<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=
	"kr.co.ohdeokrionline.model.dao.ScheduleRecord_Dao">
	<select id="mySchedule" resultType="kr.co.ohdeokrionline.model.vo.ScheduleRecord_DTO">
		select user_id,pl_date,pl_sub,pl_content,pro_name from plan where user_id=#{user_id}
	</select>
	<insert id="scheduleAdd" parameterType="kr.co.ohdeokrionline.model.vo.ScheduleRecord_DTO">
		insert into
		plan(user_id,pl_date,pl_sub,pl_content,pro_name)
		values(#{user_id},to_date(#{pl_date},'yyyy-MM-dd'),#{pl_sub},#{pl_content},#{pro_name})
	</insert>
</mapper>